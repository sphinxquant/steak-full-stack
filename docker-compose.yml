---
version: '3.4'
services:
  sponsered-by-dev:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        UID: ${UID:-1000}
        GID: ${GID:-1000}
        USER_NAME: ${DOCKER_USER:-dockeruser}
    image: sponsered-by-dev:latest
    working_dir: /usr/src/app
    volumes:
      - ${PWD}:/usr/src/app
    ports:
      - 19234:19234
    restart: 'no'
